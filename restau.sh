#!/bin/bash

    #sudo apt-get update
    #sudo apt-get install dselect
    #sudo dselect update
   #sudo dpkg --set-selections
    #sudo apt-get -u dselect-upgrade
tar -xvf /home/ecodair/Bureau/sauvegarde.tar.gz

